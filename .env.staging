# 预发布环境的环境变量（命名必须以 VITE_ 开头）

## 后端接口地址（如果解决跨域问题采用 CORS 就需要写绝对路径）
VITE_BASE_URL = https://sit-apifoxmock.com

## 是否使用 mock 接口（true: 使用 mock，false: 使用测试/生产环境）
VITE_USE_MOCK = false

## 打包构建静态资源公共路径（例如部署到 https://un-pany.github.io/ 域名下就需要填写 /）
VITE_PUBLIC_PATH = /
